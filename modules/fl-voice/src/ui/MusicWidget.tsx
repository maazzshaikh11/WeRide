/**
 * Music Control Widget (P2 — cut first if behind schedule).
 * Spotify Web API or Apple MusicKit. DO NOT start until P0/P1 across all 4 are done.
 * Ported from music_widget.dart.
 *
 * TODO: everything — only build if Weeks 1-5 finish early.
 */

import React from 'react';
import { View } from 'react-native';

export default function MusicWidget() {
  return <View />; // stub — not implemented
}